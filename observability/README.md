# Observability

Área reservada para logs, traces, métricas, correlação e evidência operacional.

Estado atual:

- a trilha local persistida é a fonte primária de auditoria;
- `LangSmith` permanece apenas como camada complementar quando configurado;
- a observabilidade já suporta auditoria de requests, `internal pilot` e comparação de paths.
