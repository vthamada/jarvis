# Evolution

Area reservada para laboratorio evolutivo, benchmarks e comparacao segura.

Estado atual:

- o `evolution-lab` permanece `sandbox-only`;
- proposals e decisoes sao persistidas localmente;
- o ciclo atual usa essa camada para avaliar sinais do `internal pilot` e da comparacao de paths, sem reabrir o baseline do `v1`.
