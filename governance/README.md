# Governance

Area reservada para politicas, avaliacao de risco, controles, auditoria e limites de autonomia.

Estado atual:

- a governanca do baseline ja diferencia `allow`, `allow_with_conditions`, `block` e `defer_for_validation`;
- no ciclo atual, essa camada continua importante para a decisao de continuidade entre missoes quando houver conflito de direcao.
