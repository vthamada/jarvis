# Knowledge

Area reservada para dominio de conhecimento, corpus curado, indexacao e retrieval.

Estado atual:

- o baseline usa corpus local curado;
- o retrieval do `v1` continua deterministico;
- memoria semantica profunda e `pgvector` permanecem fora do caminho critico do ciclo atual.
